/**元组
 * 元组类型时另一种类型的数组，它确切的知道包含多少个元素，以及特定索引对应的类型
 * 元组类型可以确切地标记出有多少个元素，以及每个元素的类型
*/

// 只限制了数组元素类型，但没限制个数
const arr:number[] = [36.5,77.8,88.9] 
//下面是元组，限制了每个元素类型，元素个数
const arr22:[number,number] = [33,66]